- lire chaque élément généré et comprendre ce qu'il se passe

- ajouter les différentes variables/quantités dans variables.py 
selon les exemples (eux-même à corriger)

Immediate next steps
1. Start small: Refactor one module at a time
2. Create Variable class and registry first
3. Build Cell and Run classes to encapsulate data
4. Move plotting to dedicated module with base classes
5. Extract fitting into separate analysis module
6. Add tests as you refactor

Code quality tips
Use type hints throughout
- Add docstrings (Google or NumPy style)
- Reduce function size: Many functions are 50+ lines
- Eliminate magic numbers: Use named constants
- Use pathlib.Path instead of string manipulation
- Consider pandas alternatives (xarray, polars) for better performance
- Add logging instead of print() statements
- Write unit tests for physics calculations